#### How to upload from Client?

```agsl
UPLOAD /path/to/file
```
* You can specify only file name.
* File name can't have spaces in them.
* File doesn't need to be in that directory. Path can contain subdirectories.